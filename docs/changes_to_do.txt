- add the option to choose between the datasets to start the exercises (to avoid computing it manually in the config file)
- Make sure the tests are reliable
- Test if new dataset memory file is created
- make it work with the phone: (Services like Railway or Fly.io let you deploy a Flask app for free (small projects). Your app lives on the internet 24/7, you get a real URL like https://vocapp.railway.app, and it works on any device anywhere with no laptop needed. you'd need a Procfile or Dockerfile but it's a genuinely useful thing to learn and your app is simple enough that it wouldn't be hard.)
- Change a bit the style with colors that you like better
